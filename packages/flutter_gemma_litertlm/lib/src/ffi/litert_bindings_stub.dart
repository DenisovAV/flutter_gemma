// Web stub for litert_bindings.dart
//
// The LiteRt interpreter FFI (dart:ffi) is native-only. Web leaves (e.g. the
// embeddings web arm) use their own JS interop (`litert_embeddings.js`) —
// no symbols from this file are used on web. This stub exists purely so the
// `flutter_gemma_litertlm.dart` conditional export compiles on web.
