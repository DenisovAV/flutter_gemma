{
  "name": "flutter-gemma-embedding-test",
  "version": "1.0.0",
  "description": "Phase 0: Web Embeddings Validation Prototype",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && mkdir -p /tmp/rag_build && cp dist/*.js /tmp/rag_build/ && vite build --config vite.config.worker.js && cp /tmp/rag_build/*.js dist/ && rm -rf /tmp/rag_build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@litertjs/core": "^0.2.1",
    "@sctg/sentencepiece-js": "^1.3.3",
    "@tensorflow/tfjs-backend-webgl": "4.9.0",
    "@tensorflow/tfjs-core": "4.9.0",
    "wa-sqlite": "github:rhashimoto/wa-sqlite"
  },
  "devDependencies": {
    "puppeteer": "^24.31.0",
    "vite": "^5.0.0",
    "vite-plugin-node-polyfills": "^0.24.0"
  }
}
