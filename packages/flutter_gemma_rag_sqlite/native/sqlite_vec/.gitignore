# Build staging — regenerated by build_local.sh; its dist/*.tar.gz ARE what
# gets uploaded to a native-sqlite-vec-v* release, so keep it out of git but
# do not treat it as scratch.
#
# prebuilt/ is ignored at the repository root — consumers fetch the loadables
# from the release; a local prebuilt/ is the maintainer path only.
dist/
